<!-- res/drawable/rounded_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Ganti warna sesuai kebutuhan -->
    <corners android:radius="16dp"/> <!-- Ukuran radius untuk membuat rounded corner -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray"/> <!-- Opsional: untuk menambahkan border -->
</shape>
